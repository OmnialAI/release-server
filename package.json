{
  "name": "release-server",
  "version": "1.0.0",
  "description": "A simple release server for software updates",
  "main": "worker.js",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler publish"
  },
  "dependencies": {
    "@aws-sdk/client-s3": "^3.832.0"
  },
  "devDependencies": {
    "wrangler": "^3.15.0"
  }
}
